{
  "name": "r2-school",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "check:server-imports": "node scripts/check-server-imports.mjs",
    "build": "node scripts/check-server-imports.mjs && prisma generate && (prisma db push --skip-generate || echo 'db push skipped') && next build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3",
    "next": "14.2.15",
    "prisma": "^6.19.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "typescript": "^5"
  }
}
